用于解决IDOR漏洞后利用获取FLAG

如果需要访问某个userid对应的资源，同时后端采用jwt验证，可以同时修改jwt令牌和id进行访问，你需要进行如下推理：
    1）当前jwt为：xxx，解码后为xxx，其中x参数与需要测试的参数有关
    2）我需要将jwt解码后的x参数修改为x，并编码，编码后的值为xxx
    3）我继续调用request工具使用新的jwt令牌与新参数进行测试。
